package Day4;

public class Encapsulation {
    public static void main(String[] args) {
        Person person = new Person();
        // set the values for the person
        person.setName("Yeswanth");
        person.setRollno(5746);
        person.setAge(19);
        System.out.println(person.getName()+" "+person.getRollno()+" "+person.getAge());
        // Instead of using the getter method from above java 15 we
    }
}
